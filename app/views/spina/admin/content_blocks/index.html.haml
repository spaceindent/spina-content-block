.well
  .dd#pages_list{data: {:"sort-url" => spina.sort_admin_pages_url }}
    %ol.dd-list
      - @blocks.each do |block|
        %li.dd-item{data: {id: block.id}}
          %div.dd-item-inner
            = render partial: 'block', object: block
